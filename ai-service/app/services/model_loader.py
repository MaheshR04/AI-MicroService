class ModelLoader:
    def __init__(self):
        self.is_loaded = False
        
    def load_weights(self):
        # Simulate loading NLP/BERT transformer weights
        self.is_loaded = True
        return "Specialist weights loaded successfully"

model_loader = ModelLoader()
